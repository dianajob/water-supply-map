export const findSensor = id => {
  const sensor = null; //sensors.find(s => s.id === id);
  return sensor;
};

export const getValue = id => {
  const sensor = null; //sensors.find(s => s.id === id);
  return sensor ? sensor.value : null;
};
